let fs = require("fs");

class Patient {
    constructor (id,nama,diagnosis) {
        this.id = id;
        this.nama = nama;
        this.diagnosis = diagnosis;
    }

    static addPatient(nama,penyakit,cb){
        
    }
}